#!/usr/bin/env bash

eval "$(docker-machine env lacqueristas)" &&
git pull origin core &&
cd ./origin && git pull origin core --rebase --autostash --commit && cd - &&
cd ./www && git pull origin core --rebase --autostash --commit && cd - &&
bin/build &&
bin/provision &&
bin/compile &&
bin/clean &&
bin/up
