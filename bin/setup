#!/usr/bin/env bash

bin/create &&
docker-machine regenerate-certs lacqueristas &&
bin/build &&
bin/up &&
sleep 2 &&
bin/origin bin/rake db:create db:migrate db:seed &&
bin/www npm run build &&
bin/clean &&
bin/restart &&
echo $DOCKER_HOST
