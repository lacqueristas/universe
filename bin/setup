#!/usr/bin/env bash

echo "=== Installing network tools ===" &&
npm install &&
bin/create &&
bin/network &&
bin/build &&
bin/compile &&
bin/provision &&
echo "=== Turning on services ===" &&
bin/up &&
bin/clean
