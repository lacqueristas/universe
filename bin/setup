#!/usr/bin/env bash

bin/create &&
docker-machine regenerate-certs lacqueristas &&
bin/build &&
rm -rf origin/tmp/pids/server.pid &&
bin/up &&
sleep 2 &&
bin/origin bin/rake db:create db:migrate db:seed &&
bin/clean &&
bin/restart &&
echo $DOCKER_HOST
