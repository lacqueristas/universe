#!/usr/bin/env bash

docker-machine regenerate-certs --force lacqueristas &&
docker-machine create --driver virtualbox --virtualbox-cpu-count 2 --virtualbox-memory 4096 lacqueristas
bin/nfs lacqueristas
