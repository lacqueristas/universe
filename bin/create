#!/usr/bin/env bash

docker-machine create --driver virtualbox --virtualbox-cpu-count 2 --virtualbox-memory 4096 --virtualbox-disk-size "50000" lacqueristas
docker-machine start lacqueristas
docker-machine regenerate-certs lacqueristas --force &&
bin/nfs lacqueristas
