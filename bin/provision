#!/usr/bin/env bash

echo "=== Turning on persistance services ===" &&
bin/up postgres &&
bin/up memcached &&

echo "=== Creating databases ===" &&
bin/run origin bin/rake --trace db:drop db:create db:migrate db:seed
